# SOURCE card activation example

Use `SOURCE` when routine `SOURCE` must sample primary particles.

```text
BEAM           <maximum-energy-context> ...
SOURCE         p1 p2 p3 p4 p5 p6 label
```

Keep units explicit, copy auxiliary spectra into the run folder, and avoid starting particles exactly on geometry boundaries.
